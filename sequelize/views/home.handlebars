<h1>Usuários Cadastrados</h1>
<div class="container">

   {{#each users}}

   <div class="item">
      <h2>{{this.name}}</h2>
      <p>{{this.ocuppation}}</p>
      <div class="actions">
         <p> <a href="/users/{{this.id}}" class="details">Ver detalhes</a></p>
         <a href="/users/editar/{{this.id}}"><input type="submit" value="Editar" class=".button3"></a>
         <form action="/users/delete/{{this.id}}" method="POST">
            <input type="submit" value="Excluir" class="delete-btn">
         </form>


      </div>
   </div>




   {{/each}}


</div>